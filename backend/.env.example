MONGODB_URI=mongodb://localhost:27017/nagar_bazaar
PORT=5000
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=7d
NODE_ENV=development
# Comma-separated list of allowed frontend origins (enforced only when NODE_ENV=production)
CORS_ORIGIN=http://localhost:3000
